import { Calendar } from "@/components/pages/calendar"

export const metadata = {
  title: "Live Time Calendar",
  description: "Event calendar management",
}

export default function CalendarPage() {
  return <Calendar />
}
