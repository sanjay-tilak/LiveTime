import { Inbox } from "@/components/pages/inbox"

export const metadata = {
  title: "Inbox — LiveTime",
  description: "View and respond to brand partnership messages",
}

export default function InboxPage() {
  return (
    <Inbox />
  )
}
